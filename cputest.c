
#include <stdio.h>
#include <stdlib.h>
#include <string.h>

int main ()
{
    int * i;
    float * p;

    while (p == p){
          (*p++)+1.144564542315345;
          (*i++)+1;
          }
}

while (fscanf(fp, "%s %s", str1, str2) != EOF ){
				strcpy(nomClient,"Linux");
				strcat(str1, " ");
				strcat(str1, str2);
				
				if(strstr(str1, nomClient)!=NULL){
					printf("%s \n",str1);
					
				}
